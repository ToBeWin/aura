{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Aura dashboard and capsule windows",
  "windows": ["dashboard", "capsule"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
